# 🏗️ System Architecture Diagrams

## 1. High-Level System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                                │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐        │
│  │   Browser 1    │  │   Browser 2    │  │   Browser N    │        │
│  │ localhost:3000 │  │ localhost:3000 │  │ localhost:3000 │        │
│  └───────┬────────┘  └───────┬────────┘  └───────┬────────┘        │
│          │                    │                    │                 │
└──────────┼────────────────────┼────────────────────┼─────────────────┘
           │                    │                    │
           │ HTTP POST          │ WebSocket          │
           │ (update_stock)     │ (real-time)        │
           │                    │                    │
┌──────────▼────────────────────▼────────────────────▼─────────────────┐
│                        APPLICATION LAYER                             │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              Flask-SocketIO Server (Port 5000)               │   │
│  │                                                               │   │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐ │   │
│  │  │  HTTP Routes   │  │ Socket Events  │  │  CORS Handler  │ │   │
│  │  │  - /health     │  │  - connect     │  │  - Origins     │ │   │
│  │  │  - /inventory  │  │  - disconnect  │  │  - Headers     │ │   │
│  │  │  - /update...  │  │  - join_room   │  │  - Methods     │ │   │
│  │  └────────┬───────┘  └────────┬───────┘  └────────────────┘ │   │
│  │           │                   │                              │   │
│  │           ▼                   ▼                              │   │
│  │  ┌────────────────────────────────────────────────────────┐ │   │
│  │  │           Inventory Handler (Thread-Safe)             │ │   │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │   │
│  │  │  │         threading.Lock()                         │ │ │   │
│  │  │  │  - Ensures atomic CSV operations                 │ │ │   │
│  │  │  │  - Prevents race conditions                      │ │ │   │
│  │  │  └──────────────────────────────────────────────────┘ │ │   │
│  │  └────────────────────────────┬───────────────────────────┘ │   │
│  └───────────────────────────────┼─────────────────────────────┘   │
└────────────────────────────────────┼─────────────────────────────────┘
                                     │
┌────────────────────────────────────▼─────────────────────────────────┐
│                          DATA LAYER                                  │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                     inventory.csv                            │   │
│  │  ┌────────────────────────────────────────────────────────┐  │   │
│  │  │  ProductID | Quantity                                   │  │   │
│  │  │  ----------|----------                                  │  │   │
│  │  │  P001      | 600                                        │  │   │
│  │  │  P002      | 450                                        │  │   │
│  │  │  P003      | 300                                        │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 2. Request-Response Flow (HTTP + WebSocket)

### Scenario: User Adds 50 Units to Product P001

```
┌──────────┐     ┌──────────┐     ┌──────────────┐     ┌──────────┐
│ Client A │     │ Client B │     │   Backend    │     │   CSV    │
└────┬─────┘     └────┬─────┘     └──────┬───────┘     └────┬─────┘
     │                │                   │                  │
     │ 1. User clicks "Add Stock"         │                  │
     │────────────────────────────────────>│                  │
     │   POST /api/update_stock           │                  │
     │   {product_id: P001, qty: 50}      │                  │
     │                │                   │                  │
     │                │        2. Acquire Lock               │
     │                │                   │◄─────────────────┤
     │                │                   │  threading.Lock()│
     │                │                   │                  │
     │                │        3. Read CSV│                  │
     │                │                   ├─────────────────>│
     │                │                   │  pd.read_csv()   │
     │                │                   │                  │
     │                │        4. Update Data                │
     │                │                   │  P001: 600→650   │
     │                │                   │                  │
     │                │        5. Write CSV                  │
     │                │                   ├─────────────────>│
     │                │                   │  df.to_csv()     │
     │                │                   │                  │
     │                │        6. Release Lock               │
     │                │                   │◄─────────────────┤
     │                │                   │                  │
     │ 7. HTTP Response                   │                  │
     │<────────────────────────────────────│                  │
     │   200 OK                           │                  │
     │   {success: true, new_qty: 650}    │                  │
     │                │                   │                  │
     │ 8. Broadcast via WebSocket         │                  │
     │◄───────────────────────────────────┼───────────────┐  │
     │   inventory_update                 │               │  │
     │                │◄──────────────────┼───────────────┘  │
     │                │  inventory_update │                  │
     │                │                   │                  │
     │ 9. UI Updates  │ 9. UI Updates     │                  │
     │   (Dashboard)  │   (Dashboard)     │                  │
     │                │                   │                  │
```

**Key Points:**
- **Steps 1-7:** HTTP request/response (synchronous)
- **Step 8:** WebSocket broadcast (asynchronous, to all clients)
- **Step 9:** All connected clients update UI in real-time

---

## 3. Component Architecture (Frontend)

```
┌─────────────────────────────────────────────────────────────────┐
│                         App.js (Main)                           │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  State Management:                                        │ │
│  │  - socket (Socket.IO instance)                            │ │
│  │  - isConnected (boolean)                                  │ │
│  │  - inventory (array)                                      │ │
│  │  - notification (object)                                  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  useEffect Hook:                                          │ │
│  │  - Initialize Socket.IO connection                        │ │
│  │  - Listen for 'initial_inventory'                         │ │
│  │  - Listen for 'inventory_update'                          │ │
│  │  - Cleanup on unmount                                     │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────┐  ┌──────────────────────────────┐   │
│  │   HomePage.js        │  │  InventoryDashboard.js       │   │
│  │  ┌────────────────┐  │  │  ┌────────────────────────┐  │   │
│  │  │ QR Input       │  │  │  │ Statistics Cards      │  │   │
│  │  │ - ProductID    │  │  │  │ - Total Products      │  │   │
│  │  │ - Auto-upper   │  │  │  │ - Total Quantity      │  │   │
│  │  └────────────────┘  │  │  │ - Low Stock Count     │  │   │
│  │  ┌────────────────┐  │  │  │ - Out of Stock Count  │  │   │
│  │  │ Quantity Input │  │  │  └────────────────────────┘  │   │
│  │  │ - Number only  │  │  │  ┌────────────────────────┐  │   │
│  │  │ - Min: 1       │  │  │  │ Search & Sort          │  │   │
│  │  └────────────────┘  │  │  │ - Search by ID         │  │   │
│  │  ┌────────────────┐  │  │  │ - Sort by ID/Qty       │  │   │
│  │  │ Action Buttons │  │  │  │ - Asc/Desc toggle      │  │   │
│  │  │ - Add Stock    │  │  │  └────────────────────────┘  │   │
│  │  │ - Sell Stock   │  │  │  ┌────────────────────────┐  │   │
│  │  │ - HTTP POST    │  │  │  │ Real-Time Table        │  │   │
│  │  └────────────────┘  │  │  │ - Product ID (mono)    │  │   │
│  │  ┌────────────────┐  │  │  │ - Quantity (colored)   │  │   │
│  │  │ Quick Fill     │  │  │  │ - Status Badge         │  │   │
│  │  │ - 10, 50, 100  │  │  │  │ - Stock Level Bar      │  │   │
│  │  └────────────────┘  │  │  └────────────────────────┘  │   │
│  └──────────────────────┘  └──────────────────────────────┘   │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Toast Notification (Conditional Render):                 │ │
│  │  - Success (green)                                        │ │
│  │  - Warning (yellow)                                       │ │
│  │  - Error (red)                                            │ │
│  │  - Auto-hide after 5 seconds                              │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. Backend Module Structure

```
┌─────────────────────────────────────────────────────────────────┐
│                        main.py (Entry Point)                    │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Initialization:                                          │ │
│  │  - Flask app                                              │ │
│  │  - SocketIO (with CORS)                                   │ │
│  │  - InventoryHandler instance                              │ │
│  │  - Register socket events                                 │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  HTTP Routes (7 endpoints):                               │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │ GET  /health                  → Health check        │  │ │
│  │  │ GET  /api/inventory           → Get all products    │  │ │
│  │  │ GET  /api/inventory/<id>      → Get one product     │  │ │
│  │  │ POST /api/update_stock        → Add or sell stock   │  │ │
│  │  │ POST /api/add_product         → Add new product     │  │ │
│  │  │ DELETE /api/remove_product/<id> → Remove product    │  │ │
│  │  │ GET  /api/forecast/<id>       → Forecast (stub)     │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  WebSocket Integration:                                   │ │
│  │  - On successful stock update → broadcast_inventory_update│ │
│  │  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ inventory_       │ │ socket_events.py │ │ inventory.csv    │
│ handler.py       │ │                  │ │                  │
│                  │ │ Functions:       │ │ ProductID,Qty    │
│ InventoryHandler │ │ - register_...   │ │ P001,600         │
│                  │ │ - handle_connect │ │ P002,450         │
│ Methods:         │ │ - handle_disc... │ │ ...              │
│ - get_inventory()│ │ - broadcast_...  │ │                  │
│ - get_product... │ │                  │ │                  │
│ - update_stock() │ │ Events:          │ │                  │
│ - add_product()  │ │ - connect        │ │                  │
│ - remove_product│ │ - disconnect     │ │                  │
│                  │ │ - join_room      │ │                  │
│ threading.Lock() │ │ - leave_room     │ │                  │
│ ┌──────────────┐ │ │                  │ │                  │
│ │ Lock Context │ │ │                  │ │                  │
│ │ with lock:   │ │ │                  │ │                  │
│ │   read_csv() │ │ │                  │ │                  │
│ │   modify()   │ │ │                  │ │                  │
│ │   write_csv()│ │ │                  │ │                  │
│ └──────────────┘ │ │                  │ │                  │
└──────────────────┘ └──────────────────┘ └──────────────────┘
```

---

## 5. Thread-Safe CSV Operations

```
┌─────────────────────────────────────────────────────────────────┐
│               Concurrent Requests (Multiple Threads)            │
└─────────────────────────────────────────────────────────────────┘
         │                    │                    │
         │ Request A          │ Request B          │ Request C
         │ (Add 10 to P001)   │ (Sell 5 from P002) │ (Add 20 to P003)
         │                    │                    │
         ▼                    ▼                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                    InventoryHandler Instance                    │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │               self.lock = threading.Lock()                │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
         │                    │                    │
         ▼                    │                    │
┌─────────────────┐           │                    │
│ Thread A        │           │                    │
│ Acquire Lock ✓  │           │                    │
│ ┌─────────────┐ │           │                    │
│ │ Read CSV    │ │           │                    │
│ │ P001: 600   │ │           ▼                    │
│ │ Modify      │ │  ┌─────────────────┐           │
│ │ P001: 610   │ │  │ Thread B        │           │
│ │ Write CSV   │ │  │ Waiting for lock│           │
│ └─────────────┘ │  │ ⏳ Blocked       │           ▼
│ Release Lock ✓  │  └─────────────────┘  ┌─────────────────┐
└─────────────────┘           │            │ Thread C        │
                              ▼            │ Waiting for lock│
                     ┌─────────────────┐   │ ⏳ Blocked       │
                     │ Thread B        │   └─────────────────┘
                     │ Acquire Lock ✓  │            │
                     │ ┌─────────────┐ │            │
                     │ │ Read CSV    │ │            │
                     │ │ P002: 450   │ │            │
                     │ │ Modify      │ │            │
                     │ │ P002: 445   │ │            │
                     │ │ Write CSV   │ │            │
                     │ └─────────────┘ │            │
                     │ Release Lock ✓  │            │
                     └─────────────────┘            │
                                                    ▼
                                           ┌─────────────────┐
                                           │ Thread C        │
                                           │ Acquire Lock ✓  │
                                           │ ┌─────────────┐ │
                                           │ │ Read CSV    │ │
                                           │ │ P003: 300   │ │
                                           │ │ Modify      │ │
                                           │ │ P003: 320   │ │
                                           │ │ Write CSV   │ │
                                           │ └─────────────┘ │
                                           │ Release Lock ✓  │
                                           └─────────────────┘
```

**Key Insight:** Lock ensures only ONE thread can read/write CSV at a time, preventing data corruption.

---

## 6. WebSocket Room Broadcasting

```
┌─────────────────────────────────────────────────────────────────┐
│                        Socket.IO Server                         │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                 Room: "inventory"                         │ │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐         │ │
│  │  │ Client SID  │ │ Client SID  │ │ Client SID  │ ...     │ │
│  │  │ abc123      │ │ def456      │ │ ghi789      │         │ │
│  │  └─────────────┘ └─────────────┘ └─────────────┘         │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                             │
                             │ socketio.emit(
                             │   'inventory_update',
                             │   data,
                             │   room='inventory',
                             │   broadcast=True
                             │ )
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ Client abc123   │ │ Client def456   │ │ Client ghi789   │
│ (Browser Tab 1) │ │ (Browser Tab 2) │ │ (Browser Tab 3) │
│                 │ │                 │ │                 │
│ socket.on(      │ │ socket.on(      │ │ socket.on(      │
│  'inventory_... │ │  'inventory_... │ │  'inventory_... │
│   setInventory()│ │   setInventory()│ │   setInventory()│
│ )               │ │ )               │ │ )               │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

**Flow:**
1. Client connects → auto-joins "inventory" room
2. Server emits to room → **all** clients receive event
3. Each client updates its local state

---

## 7. Error Handling Flow

```
┌──────────┐     ┌──────────────┐     ┌──────────┐
│  Client  │     │   Backend    │     │   CSV    │
└────┬─────┘     └──────┬───────┘     └────┬─────┘
     │                  │                  │
     │ POST /api/update_stock              │
     │ {product_id: P999, qty: 10}         │
     │─────────────────>│                  │
     │                  │                  │
     │                  │ 1. Validate input│
     │                  │  (Missing fields?│
     │                  │   Invalid action?│
     │                  │   Qty <= 0?)     │
     │                  │                  │
     │                  │ 2. Call handler  │
     │                  ├─────────────────>│
     │                  │  Read CSV        │
     │                  │                  │
     │                  │ 3. Check product │
     │                  │  exists?         │
     │                  │  (P999 NOT FOUND)│
     │                  │                  │
     │                  │ 4. Return error  │
     │                  │◄─────────────────┤
     │                  │  {success: false}│
     │                  │                  │
     │ 5. HTTP 400      │                  │
     │◄─────────────────│                  │
     │  {success: false,│                  │
     │   message: "..."}│                  │
     │                  │                  │
     │ 6. Show toast    │                  │
     │  (Red error)     │                  │
     │                  │                  │
```

**Error Types:**
- **400 Bad Request:** Validation errors, insufficient stock
- **404 Not Found:** Product doesn't exist
- **500 Internal Server Error:** Unexpected exceptions

---

## 8. State Management (React)

```
┌─────────────────────────────────────────────────────────────────┐
│                        App.js (Root)                            │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  useState Hooks:                                          │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │ const [socket, setSocket] = useState(null)          │  │ │
│  │  │ const [isConnected, setIsConnected] = useState(false│  │ │
│  │  │ const [inventory, setInventory] = useState([])      │  │ │
│  │  │ const [notification, setNotification] = useState(...)│  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  useEffect (WebSocket Setup):                             │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │ const socket = io('http://localhost:5000')          │  │ │
│  │  │ setSocket(socket)                                   │  │ │
│  │  │                                                     │  │ │
│  │  │ socket.on('connect', () => {                        │  │ │
│  │  │   setIsConnected(true)                              │  │ │
│  │  │ })                                                  │  │ │
│  │  │                                                     │  │ │
│  │  │ socket.on('inventory_update', (data) => {           │  │ │
│  │  │   setInventory(data.inventory) ───┐                │  │ │
│  │  │   showNotification(data.message)  │                │  │ │
│  │  │ })                                │                │  │ │
│  │  └───────────────────────────────────┼────────────────┘  │ │
│  └───────────────────────────────────────┼────────────────────┘│
└─────────────────────────────────────────┼──────────────────────┘
                                          │
                                          │ Trigger Re-render
                                          ▼
                           ┌──────────────────────────┐
                           │ InventoryDashboard.js    │
                           │ (Receives inventory prop)│
                           │                          │
                           │ useEffect(() => {        │
                           │   filterAndSort()        │
                           │ }, [inventory, ...])     │
                           │                          │
                           │ → Table re-renders       │
                           └──────────────────────────┘
```

---

**End of Architecture Diagrams** 🎯
